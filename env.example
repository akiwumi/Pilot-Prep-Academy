# Copy to .env.local and fill in values
#
# Gemini (already used by vite.config.ts)
GEMINI_API_KEY=
#
# Supabase (Vite requires VITE_ prefix to expose to client)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=


